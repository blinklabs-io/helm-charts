# chart-testing values: block producer keys supplied by a Secret that is managed
# outside of this chart, for example by an ExternalSecret. No blockProducer.keys
# are given, the chart must not render a keys Secret, and the pod must mount the
# named Secret at /keys.
blockProducer:
  enabled: true
  existingSecret: dingo-ci-block-producer-keys
