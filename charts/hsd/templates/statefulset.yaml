---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    {{- include "hsd.labels" . | nindent 4 }}
  name: {{ include "hsd.network-fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      handshake_network: {{ include "hsd.network" . }}
      app.kubernetes.io/name: {{ include "hsd.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  serviceName: {{ include "hsd.network-fullname" . }}
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
        kubectl.kubernetes.io/default-container: hsd
      labels:
        handshake_network: {{ include "hsd.network" . }}
        app.kubernetes.io/name: {{ include "hsd.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        {{- include "hsd.labels" . | nindent 8 }}
    spec:
      {{- with .Values.affinity }}
      affinity: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{ toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroup: 1000
      containers:
      - name: hsd
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        args:
        - --network={{ include "hsd.network" . }}
        - --listen={{ .Values.nodeConfig.listen }}
        - --max-inbound={{ .Values.nodeConfig.maxInbound }}
        - --max-outbound={{ .Values.nodeConfig.maxOutbound }}
        - --http-host={{ .Values.nodeConfig.httpHost }}
        - --http-port={{ include "hsd.http-port" . }}
        {{- if .Values.nodeConfig.api.secretName }}
        - --api-key=$(HSD_API_KEY)
        {{- end }}
        {{- if .Values.nodeConfig.publicHost }}
        - --public-host={{ .Values.nodeConfig.publicHost }}
        {{- end }}
        - --brontide-port={{ include "hsd.brontide-port" . }}
        - --prune={{ .Values.nodeConfig.prune }}
        - --index-tx={{ .Values.nodeConfig.indexTx }}
        - --index-address={{ .Values.nodeConfig.indexAddress }}
        - --checkpoints={{ .Values.nodeConfig.checkpoints }}
        - --log-level={{ .Values.nodeConfig.logLevel }}
        - --cors={{ .Values.nodeConfig.cors }}
        {{- if .Values.nodeConfig.ssl }}
        - --ssl=true
        - --ssl-cert={{ .Values.nodeConfig.sslCert }}
        - --ssl-key={{ .Values.nodeConfig.sslKey }}
        {{- end }}
        {{- if not .Values.nodeConfig.walletEnabled }}
        - --no-wallet
        {{- end }}
        - --ns-host={{ .Values.nodeConfig.nsHost }}
        - --ns-port={{ include "hsd.ns-port" . }}
        - --rs-host={{ .Values.nodeConfig.rsHost }}
        - --rs-port={{ include "hsd.rs-port" . }}
        - --rs-no-unbound={{ .Values.nodeConfig.rsNoUnbound }}
        env:
        {{- if .Values.nodeConfig.api.secretName }}
        - name: HSD_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.nodeConfig.api.secretName }}
              key: {{ .Values.nodeConfig.api.secretKey }}
        {{- end }}
        - name: HSD_NETWORK
          value: "{{ include "hsd.network" . }}"
        - name: HSD_PREFIX
          value: "/data"
        ports:
        - name: p2p
          containerPort: {{ include "hsd.p2p-port" . }}
          protocol: TCP
        - name: brontide
          containerPort: {{ include "hsd.brontide-port" . }}
          protocol: TCP
        - name: http
          containerPort: {{ include "hsd.http-port" . }}
          protocol: TCP
        - name: ns
          containerPort: {{ include "hsd.ns-port" . }}
          protocol: UDP
        - name: rs
          containerPort: {{ include "hsd.rs-port" . }}
          protocol: UDP
        resources:
          {{- $network := include "hsd.network" . }}
          {{- if .Values.resources }}
          {{- toYaml .Values.resources | nindent 12 }}
          {{- else }}
          {{- toYaml (index .Values.networkResources $network) | nindent 12 }}
          {{- end }}
        volumeMounts:
        - name: handshake-data-{{ include "hsd.network" . }}
          mountPath: /data
      {{- if .Values.metrics.enabled }}
      - name: hsd-metrics-exporter
        image: "{{ .Values.metrics.image.repository }}:{{ .Values.metrics.image.tag }}"
        imagePullPolicy: {{ .Values.metrics.image.pullPolicy | default "IfNotPresent" }}
        env:
        - name: HANDSHAKE_RPC_HOST
          value: "localhost"
        - name: HANDSHAKE_RPC_PORT
          value: "{{ include "hsd.http-port" . }}"
        - name: HANDSHAKE_RPC_USER
          value: "x"
        - name: HANDSHAKE_RPC_PASSWORD
          {{- if .Values.nodeConfig.api.secretName }}
          valueFrom:
            secretKeyRef:
              name: {{ .Values.nodeConfig.api.secretName }}
              key: {{ .Values.nodeConfig.api.secretKey }}
          {{- else }}
          value: ""
          {{- end }}
        - name: REFRESH_SECONDS
          value: {{ .Values.metrics.refreshSeconds | quote }}
        - name: BAN_ADDRESS_METRICS
          value: {{ .Values.metrics.banAddressMetrics | quote }}
        - name: LOG_LEVEL
          value: "{{ .Values.metrics.logLevel }}"
        ports:
        - name: metrics
          containerPort: {{ .Values.metrics.port }}
        readinessProbe:
          httpGet:
            path: /
            port: metrics
        resources:
          {{- toYaml .Values.metrics.resources | nindent 10 }}
      {{- end }}
  {{- if .Values.updateStrategy }}
  updateStrategy:
    type: {{ .Values.updateStrategy.type }}
  {{- end }}
  volumeClaimTemplates:
  - metadata:
      labels:
        handshake_network: {{ include "hsd.network" . }}
      name: handshake-data-{{ include "hsd.network" . }}
    spec:
      accessModes:
      - ReadWriteOnce
      {{- if .Values.storage.storageClassName }}
      storageClassName: {{ .Values.storage.storageClassName }}
      {{- end }}
      resources:
        requests:
          storage: {{ include "hsd.storage-size" . }}
