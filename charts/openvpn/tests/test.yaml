apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openvpn.fullname" . }}-test"
  labels:
    {{- include "openvpn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: busybox
    command: ['sh', '-c', 'echo "OpenVPN chart test passed"']