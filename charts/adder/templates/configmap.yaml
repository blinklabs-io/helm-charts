---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "adder.fullname" . }}-config
  labels:
    {{- include "adder.labels" . | nindent 4 }}
data:
  config: |-
    {{ .Values.config | nindent 4 }}
