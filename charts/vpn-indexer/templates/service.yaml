---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vpn-indexer.fullname" . }}
  labels:
    {{- include "vpn-indexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vpn-indexer.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vpn-indexer.fullname" . }}-sts
  labels:
    {{- include "vpn-indexer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vpn-indexer.selectorLabels" . | nindent 4 }}
